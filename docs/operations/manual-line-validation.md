# 手動返信移行の検証記録

実施日: 2026-09-07。対象: `hori0926/line-harness-oss` PR #1 の手動運用追加。

## 自動検証

- ワークスペースのテスト: 1,992件成功。
- スクリプトのテスト: 58件成功（Cloudflare設定・DB移行の4件を含む）。
- Worker型チェック、Workerビルド、管理画面の本番ビルド（50ページ）: 成功。
- 実workerd/D1/R2: 小容量・5MiB境界・複数パート・容量超過、担当ロックの競合と期限、受信重複排除、引用トークン保持、別担当者の送信拒否、送信後DB障害からの再試行、プロフィール復旧、添付復旧を確認。
- 添付復旧は元の受信日時を保持し、`GET /api/chats/:id?since=...` が更新内容を返すことまで実D1で確認。

LINEへの送信はモック。実LINEの配達や本番Cloudflareの稼働を証明するものではありません。

## PCブラウザ

ローカルWorker・Queue・D1・R2とLINEモック、管理画面を接続して確認。

- テキスト受信、引用返信、送信者名の表示。
- メモ保存、解決済への変更。
- 動画・音声の読み込み、再生、シーク。
- 日本語名PDFのダウンロードとPDFデータの確認。
- 担当Aが対応中に担当Bの画面で担当者名が表示され、送信が無効になる。
- 最終確認時、ブラウザコンソールエラーなし。

## 実環境の未完了項目

Cloudflare認証、専用リソース作成、Worker secrets、テスト用LINE接続、実端末への送信、担当者による受入確認は未完了。
GitHubのリソース名の既定値は登録済み、自動デプロイは未設定環境へ走らないよう無効。

[移行手順と実LINEでの受入チェック](manual-line-migration.md)に従って完了後に業務を切り替える。
Push APIの配信通数、過去履歴・既存タグの扱い、普段使う送信形式の確認が必要。
