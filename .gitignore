node_modules/
dist/
dist-mcp/
*.tsbuildinfo
.next/
.wrangler/
.vercel/
.env
.env.local
*.log
.DS_Store
.mcp.json
*.toml.bak
.env.production
.env.staging
# Wrangler local secrets — matches apps/worker/.dev.vars and any other worker's copy at any depth
.dev.vars
.dev.vars.*
apps/web/out/
apps/worker/dist/
.worktrees/
.claude/worktrees/
.line-harness-setup.json
.line-harness-config.json

# Wrangler local secrets — matches apps/worker/.dev.vars and any other worker's copy at any depth
.dev.vars
.dev.vars.*

# Claude Code local dev-server config (machine-local paths)
.claude/launch.json

# ローカル開発用に生成するテストメディア (scripts/dev/make-fixtures.mjs の出力)。
# 生成物のバイナリなのでコミットしない — スクリプトを実行すれば再生成できる。
scripts/dev/fixtures/
.gstack/

# Isolated local QA builds
.next-manual-qa/
